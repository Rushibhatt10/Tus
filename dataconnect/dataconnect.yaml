specVersion: "v1"
serviceId: "newfolder3"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "newfolder3-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
